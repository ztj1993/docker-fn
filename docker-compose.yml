version: "3"
services:
  fn:
    build:
      context: .
      dockerfile: Dockerfile
    image: ztj1993/fn:latest
    volumes:
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
